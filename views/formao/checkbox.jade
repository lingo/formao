.checkbox
    block inputField
        label(for=attr.id)
            if value
                - attr.checked = true;
            input(type="checkbox",name=name,value=1).checkbox&attributes(attr)
            | #{label}